## 从群到空间再到物理规律的思考历程



**主要内容**

1.近期的学习过程以及收获

2.对Clifford代数的基本入门和辫子群等

3.一些小实例



参考文献：

``` 
近世代数 ---丘维声
从群到李代数 ---冯承天
AN INTRODUCTION TO CLIFFORD ALGEBRAS AND SPINORS --Jayme Vaz, Jr ; Roldão da Rocha, Jr
微分几何引论 --陈维桓
Topological Quantum Computation ---Zhenghan Wang
```



#### 1.学习过程和想法

学习的大概过程： 先去了解底层的Clifford代数，但是这一部分非常繁多，而且到后面基本都是微分几何相关内容了，相对于要用到的辫子群而言，只有前面的部分知识，如内积，外积运算，生成关系这些联系多。因此我选择先学习最基本的，然后看到和物理相关的，就转移到物理相关的方面。



简单体会了研究空间性质的思路，以及如何从群到空间再到物理：

1.为什么要研究群：

从应用的视角来看：一些变换或者其他操作，大部分都可以化为某些基本的简单变换的复合。如果能把变换这个概念本身给抽象出来，把复合当作一种运算，研究变换就会变得方便。

另一方面运算若是能表示成矩阵形式，再对应群表示论相关内容，可以通过同态变换的保运算性质，从基本的群来向上推导出更多的变换性质。比如从某个群的子群可以推广到该群的诱导映射等。

一些实际的例子，如$SU(2)$， 可以通过四元数来诱导出$SO(3)$这样一个旋转变换，而$SO(3)$可以表示三维空间中的旋转。换句话说就是用四元数来研究旋转:

$p=x*i+y*j+z*k 同构为 x\vec i+y\vec j+z\vec k$这样的$R^3$空间中的向量，就给出了四元数的矩阵表示，进一步可以构造泡利矩阵等物理相关的东西。



2.进一步把群和空间联系起来

典型的例子，如旋转操作，在Clifford代数中可以通过 $\sigma \omega \sigma^{-1} $ 来实现。反过来，推理空间的基本性质可以通过构建基本群来实现。

例如从代数上构建一下辫群：利用基本群来构建空间。基本群作为同伦不变量，构建拓扑空间时可以考虑研究基本群，进一步做等价类就能得出空间的性质。



假设$C$上有$n$个点，在不区分顺序情况下，让它们在C上运动，但是不重合在一起。从而构成一个空间$X$,可以研究这个空间的基本群。

首先$n$个点都属于$C$， 两两不重合，从全空间排除重合情况
$$
X'={(z_1,...,z_n)\in C^n|z_i\ne z_j,i\ne j} =C^n\setminus  {z_i=z_j | i\ne j}
$$
不区分顺序时，即找到置换群的等价类：
$$
X=(C^n\setminus {z_i=z_j|z_i\ne z_j})/S_n
$$
但是这个空间不容易写出来, 可以考虑用两个复平面画出来：

![img](https://picx.zhimg.com/v2-ae7fa28d440a51f7449f54241e769069_r.jpg?source=1def8aca)



辫子可以更加复杂，如$n$个点组成的辫子群$B_n$

把辫子连起来的操作视为复合，如下

![img](https://picx.zhimg.com/v2-521ee06dfb6badb4ce60d92049b1ba6c_r.jpg?source=1def8aca)



从几何上研究其关系：

- 远距离可换: $σ_i σ_j = σ_j σ_i$, 若$ |i-j| \ge 2$
- 三元关系（Yang–Baxter 型）: $σ_i σ_{i+1} σ_i = σ_{i+1} σ_i σ_{i+1}$

通过这些生成关系就可以构建出这个空间的基本群,很显然$n\ge 3$以后辨群$B_n$就是非阿贝尔群了

最后的三元关系可以跟纽结理论的生成关系联系，但是时间不够没有细看👉👈 .另外辫子也可以应用给代数曲线，也可以和判别式等于0的代数方程同构...



#### 2.物理和Clifford代数

Clifford代数内容很宽泛，建立了一套完整的几何变换的描述。比如上面提到的旋转，诸如$R^2,R^3$上的旋转都可写成更大代数的乘积，比如$R^3$中可以使用单位四元数来描述旋转,$h\longmapsto qh\bar q$。而物理中最常见的对称化和反对称化运算，在Clifford中也可以由内积和外积来描述:

$Def: (V,<\cdot,\cdot>)$是一个$n$维内积空间，接着定义Clifford代数：
$$
CL(V):=\frac{TV}{(x\otimes x+<x,x>\cdot 1|x\in V)}
$$
TV为V的张量代数

即Clifford满足生成关系：$x\otimes x=-<x,x>\cdot 1=-|x|^2\cdot1$

于是有 $v ⊗ u + u ⊗ v = − 2<v, u>\cdot1$ , 因此，如果记一组标准正交基 ${x_i}_{1\le i\le n}$，有：

$CL(V)是由x_1,...,x_n生成的一个代数，满足生成关系： x_i\cdot x_i=-1, x_i\cdot x_j=-x_j\cdot x_i$

当然如果生成元选的是复的就会出现相乘是1,下面要用到的Majorana算子就是这样



可以记向量乘积：$v  u = v ∧ u + <v, u>$ ，类似得到$ v ∧ u ∧ w = \frac{1}{3} (v ⊗ (u ∧ w) − u ⊗ (v ∧ w) + w ⊗ (v ∧ u))$等等

实际的例子比如：

1: $V=R$,记V的生成元是$x$， 则$CL(R)=R[x]/(x^2+1)\cong C$

2: $V=R^2$,标准正交基$x,y$，因此满足$x^2=y^2=-1,xy=-yx$,于是可以令$x=i,y=j,xy=k$ ，故有 $CL(R^2)\cong H$，及表示0了纯四元数

...

以后可以推广出旋量几何等等。



物理上的联系：

如：Dirac 方程 $(iγ^μ ∂_μ - m)ψ = 0$，γ 矩阵满足 ${γ^μ,γ^ν}=2η^{μν}$

以及Majorana fermion:

首先是费米子关系式：

$ f =  \tfrac{1}{\sqrt{2}} (\gamma_1+i\gamma_2),$
$ f^{\dagger} =  \tfrac{1}{\sqrt{2}} (\gamma_1-i\gamma_2) ~.$

而Majorana费米子满足: $\{γ_i, γ_j\} = γ_i γ_j + γ_j γ_i = 2 δ_{ij}, γ_i† = γ_i.$

这是实 Clifford 代数，由一组满足上述反对易关系的生成元构成。所以从代数角度看，Majorana 系统就是在操作一个 Clifford 代数的表示

从 Majorana 到复费米子与 Hilbert 空间维度

- 把两个 Majorana 配对成一个常规复杂费米子：
    $c = (γ_1 + i γ_2)/2, c† = (γ_1 - i γ_2)/2.$
- 若有 $2n$ 个 Majorana，则可构造 n 个复费米子，整个费米子希尔伯特空间大小为 $2^n$。通过张量积乘在一起连接起来。

设一组Majorana算子$\{c_n\}$

构造辫子算子：$\sigma_k=(1+c_{k+1}c_k)\sqrt2, $   $\sigma_k^{-1}=(1-c_{k+1}c_k)\sqrt2 $    ,$k=1,2...n$,定义$c_{n+1}=c_1$

则有$σ_i σ_j = σ_j σ_i$, 若$ |i-j| \ge 2$，  $σ_i σ_{i+1} σ_i = σ_{i+1} σ_i σ_{i+1}$   ------> 辨群$B_n$

辫子操作： $T_k(x)=\sigma_k x \sigma_k^{-1}$ , $T_k(c_k)=c_{k+1}, T_k(c_{l+1})=-c_k ;T_k(c_i)=c_i ,\space if\space i \ne k, k+1 $



推广： 对于一组Clifford代数里的算子$M_i$，都可以用$\sigma_i=(1+M_i)/\sqrt2$来沟槽Artin辫子群，

比如用四元数 $A=\frac{1}{\sqrt2}(1+i),...$ 



#### 3.拓扑量子计算

个人理解： 拓扑量子计算利用二维的任意子，利用辨群结构充当逻辑门来进行计算。（图片来自wiki）

![undefined](https://upload.wikimedia.org/wikipedia/commons/0/0f/Topological_quantum_computer.jpg)

**The unitary operation corresponding to exchanging anyons depends only on the topology of the braid**



Anyons：

任意子是对费米子和玻色子概念的广义化，其交换可以产生任意相位或更一般的幺正变换。



Abel anyons：$\mid\psi_1\psi_2>=e^{i\theta}\mid\psi_2\psi_1>$

Non abel anyons： 不同交换顺序不一定可交换;  

结合代数：

 $a×b=∑_cN^a_{bc} c（N^a_{bc}为非负整数）$

辫子表示：给定 $n$个任何子，辫子群 $B_n$在结合空间 $H$上有表示 $ρ:Bn→U(H)$；每个生成元 $σi$（交换第 i 与 i+1粒子）映为幺正矩阵 $ρ(σi)$。

计算 $σi$的常用步骤：若在当前 fusion tree 下直接对角，则 $σi=diag⁡(R)$；否则用 F变基再施加 diag⁡(R)，再变回原基：$σi=F−1 diag⁡(R)$ 。 其中：

- F是多粒子结合顺序变换的矩阵：决定不同融合顺序下态的展开系数
- R给出交换两个任何子的局部相位或矩阵元：决定把两个任何子绕动一圈的局部效果

例如Fibonacci anyon: {1,τ}，$τ×τ=1+τ$，其辫子表示在逻辑子空间上是稠密的，单靠编辫子即可近似任意单比特幺正（即拓扑上普适）。



和拓扑的关系：

在任何二维以上的空间里，自旋统计定理规定任何多粒子状态都必须要么遵循费米-狄拉克统计，要么遵循玻色-爱因斯坦统计。这与$n>2$的$SO(n,1)$基本群有关，其值为$Z_2$。因此这里只有两个可能性

在二维空间里情况发生了变化，这里SO(2,1)的基本群是Z。详细地说特殊正交群SO(2,1)的射影表示不仅仅有SO(2,1)或者其二重复盖群旋量群Spin(2,1)的线性表示。而这些额外的表示被称为任意子。

这个概念对非相对论系统也有效。关键是空间旋量群是有无限基本群的SO(2)。

在二维中两个粒子的排列群不再是对称群$S_2$，而是辫子群$B_2$了。
